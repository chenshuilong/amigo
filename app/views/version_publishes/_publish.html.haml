#version-publish-add-app
  = form_tag('', {method: :get, class: 'form-horizontal', remote: true}) do |f|
    .issue-details
      #all_attributes
        - title_hash.each do |k, v|
          .form-group
            = v
            = check_box_tag "title[#{k.to_s}]", nil, checked: true
%h2= l(:label_confirmation)
.warning
  %p= simple_format l(:text_account_destroy_confirmation)
  %p
    = form_tag({}) do
      %label
        = check_box_tag 'confirm', 1
        = l(:general_text_Yes)
      = submit_tag l(:button_delete_my_account)
      |
      \#{link_to l(:button_cancel), :action => 'account'}

- provide :css, "bootstrap"

= render :layout => 'layouts/file' do
  = render :partial => 'common/image', :locals => {:path => download_named_upload_files_url(@attachment, @attachment.filename), :alt => @attachment.filename}

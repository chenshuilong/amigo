- if @wiki && @wiki.sidebar
  .wiki
    = textilizable @wiki.sidebar.content, :text
%h3= l(:label_wiki)
%ul
  %li= link_to(l(:field_start_page), {:action => 'show', :id => nil})
  %li= link_to(l(:label_index_by_title), {:action => 'index'})
  %li= link_to(l(:label_index_by_date),                 |
      {:controller => 'wiki', :project_id => @project, |
      :action => 'date_index'})                        |

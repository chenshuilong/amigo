= title [l(("template_role_type_"+@type).to_sym), templates_path], @temp.role.name
= labelled_form_for :template, @temp do |f|
  = f.hidden_field :role_type
  = render :partial => 'form', :locals => {:f => f}
  = submit_tag l(:button_save)